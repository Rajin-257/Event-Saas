<%- include('partials/header') %>

<div class="row">
  <div class="col-md-8 mx-auto text-center">
    <div class="jumbotron my-5">
      <h1 class="display-4">Welcome to Auth App</h1>
      <p class="lead">A secure authentication system built with Node.js, Express, and MySQL</p>
      <hr class="my-4">
      <p>This application features user registration, email verification, password reset, and profile management.</p>
      <div class="mt-4">
        <% if (!isAuthenticated) { %>
          <a href="/auth/register" class="btn btn-primary btn-lg me-2">Register</a>
          <a href="/auth/login" class="btn btn-outline-primary btn-lg">Login</a>
        <% } else { %>
          <a href="/user/dashboard" class="btn btn-primary btn-lg">Go to Dashboard</a>
        <% } %>
      </div>
    </div>
  </div>
</div>

<div class="row mt-5">
  <div class="col-md-4">
    <div class="card mb-4">
      <div class="card-body text-center">
        <i class="fas fa-lock fa-3x mb-3 text-primary"></i>
        <h5 class="card-title">Secure Authentication</h5>
        <p class="card-text">Passwords are securely hashed and stored using bcrypt. JWT tokens ensure secure sessions.</p>
      </div>
    </div>
  </div>
  <div class="col-md-4">
    <div class="card mb-4">
      <div class="card-body text-center">
        <i class="fas fa-envelope fa-3x mb-3 text-primary"></i>
        <h5 class="card-title">Email Verification</h5>
        <p class="card-text">Email verification ensures that only real users can register and access the system.</p>
      </div>
    </div>
  </div>
  <div class="col-md-4">
    <div class="card mb-4">
      <div class="card-body text-center">
        <i class="fas fa-user-shield fa-3x mb-3 text-primary"></i>
        <h5 class="card-title">User Management</h5>
        <p class="card-text">Complete user profile management with avatar uploads and password reset functionality.</p>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>